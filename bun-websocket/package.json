{
  "name": "bun-websocket",
  "version": "1.0.0",
  "description": "Real-time WebSocket chat server built with Bun, optimized for Railway deployment",
  "main": "src/index.ts",
  "scripts": {
    "dev": "bun run --watch src/index.ts",
    "start": "bun run src/index.ts",
    "build": "bun build src/index.ts --outdir=dist --target=node",
    "test": "bun test",
    "type-check": "tsc --noEmit"
  },
  "dependencies": {},
  "devDependencies": {
    "bun-types": "latest",
    "typescript": "^5.2.2"
  },
  "engines": {
    "bun": ">=1.0.0"
  },
  "keywords": [
    "bun",
    "railway",
    "websocket",
    "realtime",
    "chat",
    "server",
    "typescript",
    "template",
    "websocket-server"
  ],
  "author": "Your Name",
  "license": "MIT"
}
