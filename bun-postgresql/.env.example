# Database Configuration
DATABASE_URL=postgres://user:password@localhost:5432/mydb

# Server Configuration
PORT=3000
NODE_ENV=development

# Railway will automatically set DATABASE_URL when you connect a PostgreSQL database
# For local development, update the DATABASE_URL with your local PostgreSQL credentials
