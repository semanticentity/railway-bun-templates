{
  "name": "bun-rest-api",
  "version": "1.0.0",
  "description": "Comprehensive REST API server built with Bun and TypeScript, optimized for Railway deployment",
  "main": "src/index.ts",
  "scripts": {
    "dev": "bun run --watch src/index.ts",
    "start": "bun run src/index.ts",
    "build": "bun build src/index.ts --outdir=dist --target=node",
    "test": "bun test",
    "type-check": "tsc --noEmit"
  },
  "dependencies": {},
  "devDependencies": {
    "bun-types": "latest",
    "typescript": "^5.2.2"
  },
  "engines": {
    "bun": ">=1.0.0"
  },
  "keywords": [
    "bun",
    "railway",
    "api",
    "rest",
    "typescript",
    "server",
    "template",
    "crud",
    "middleware"
  ],
  "author": "Your Name",
  "license": "MIT"
}
